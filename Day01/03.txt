Step 1: Create Node Project
        > mkdir node-hello
        > cd node-hello
        > npm init -y
        > npm install express

Step 2: Create index.js 


Step 3: Run Node Server
        > node index.js 
    
        http://localhost:3000/

        Hello World from Node JS
        
🎯 Minimal Version (for Beginners)
=================================
For small demos:

node-hello/
│
├── index.js
├── routes.js
├── db.js
└── package.json

Real Time Project Structure
===========================
node-hello/
│
├── node_modules/
├── package.json
├── package-lock.json
├── .gitignore
│
├── index.js            👉 Main entry file
│
├── routes/             👉 API routes
│   └── userRoutes.js
│
├── controllers/        👉 Request logic
│   └── userController.js
│
├── services/           👉 Business logic
│   └── userService.js
│
├── models/             👉 DB schemas / data models
│   └── userModel.js
│
├── config/             👉 DB & env config
│   └── db.js
│
├── middlewares/        👉 Auth / logging / validation
│   └── authMiddleware.js
│
├── utils/              👉 Helper functions
│   └── logger.js
│
└── .env                👉 Environment variables

📌 Folder-wise Explanation:
🔵 index.js
        App starting point.
        Creates Express server.
        Loads routes and middleware.

📁 routes/
        Defines API endpoints.
        Example: 
        router.get("/users", getUsers);

📁 controllers/
        Handles HTTP request & response.
        Calls service layer.

📁 services/
        Contains business logic.
        Talks to models.

📁 models/
        Database schemas (MongoDB / Sequelize / MySQL).
        Data structure.

📁 config/
        Database connection.
        Environment configs.

📁 middlewares/
        JWT auth
        Logging
        Error handling

📁 utils/
        Reusable helper methods.

📁 .env
        Stores secrets:
        PORT=3000
        DB_URL=mongodb://localhost:27017/app

👨‍🏫 Interview Line You Can Say
============================
In Node.js, 
we follow MVC style: 
routes handle URLs, 
controllers manage request logic, 
services contain business rules, 
and models handle database operations.

